22. Ders


enum class Weekday {
	Sunday, Monday, Tuesday, Wednesday, Thursday,
	Friday, Saturday
};

std::ostream& operator<<(std::ostream& os, const Weekday& wd)
{
	constexpr const char* pdays[] = { "Sunday","Monday",
	"Tuesday","Wednesday","Thursday","Friday","Saturday" };
	return os << pdays[static_cast<int>(wd)];
}


int main()
{

	Weekday wd(Weekday::Friday);

	std::cout << wd;
}


Weekday& operator++(Weekday& wd)
{
	using enum Weekday;

	wd = (wd == Weekday::Saturday)
		? Weekday::Sunday
		: static_cast<Weekday>(static_cast<int>(wd) + 1);

	return wd;
}

Weekday operator++(Weekday& wd,int)
{
	auto ret = wd;
	++wd;
	return ret;
}

int main()
{

	Weekday wd(Weekday::Friday);

	std::cout << wd << '\n';

	++wd;
	wd++;
	std::cout << wd << '\n';



}
Weekday operator++(Weekday& wd,int)
{
	auto ret = wd;
	++wd;
	return ret;
}
Weekday operator--(Weekday& wd,int)
{
	auto ret = wd;
	--wd;
	return ret;
}

----
class Myclass
{
public:
    Myclass() = default;
    Myclass(int x) : mx{x} {
        std::cout << "Constructor\n";
	}
    ~Myclass() {
        std::cout << "Destructor\n";
    }


private:
    int mx{};
};

	Myclass m1{ 22 }, m2{ 33 };

    auto val = m1 + m2;

	Burasý geçersiz çünkü + operatörü Myclass için tanýmlý deðil.
	fakat operator()int için tanýmlý olsaydý
	auto val = m1( ) + m2( );
	    operator int()const {
        return mx;
	}
	auto val = m1.operator int() + m2.operator int(); // int + int olacaktý


	----------------------------
	// reference qualification 
class Myclass
{
public:
    void foo();

private:
    int mx{};
};


    /*
    * istiyorum ki foo fonksiyonu
	* l value ile cagrildiginda bir sey yapsin
	* r value ile cagrildiginda baska bir sey yapsin
    */
    Myclass m;
    m.foo();
	Myclass().foo();
	std::move(m).foo();

void foo()&;  // l value referans ile cagirilinca 
sadece l valueler için çaðrýlabilir.
void foo()&&; // r value referans ile çaðrýlabilir.

// reference qualification 
class Myclass
{
public:
	void foo()&&
	{
		std::cout << "Myclass::foo r value cagrildi" << std::endl;
	}
	void foo()&
	{
		std::cout << "Myclass::foo l value cagrildi" << std::endl;
	}

private:
    int mx{};
};
    Myclass m;
    m.foo();
	Myclass().foo();
	std::move(m).foo();
	Myclass::foo l value cagrildi
Myclass::foo r value cagrildi
Myclass::foo r value cagrildi

	void foo()const&
	{
		std::cout << "Myclass::foo const l value cagrildi" << std::endl;
	}

	-----------------------------
----------------------------- NAMESPACE -----------------------------
namespace (isim alaný) nedir ?
isimler(idenrifiers)

// global namespace

// neco namespace
namespace neco {
	int  x = 42;

}

Yani aslýnda global -> neco -> x
::neco::x  ...  neco::x 
:: global namespace i temsil eder.

namespace class ve fonksiyonlarýn içinde tanýmlanamaz.

namespace erg {
	namespace MyNamespace
	{
		namespace a {
			int x = 100;
		}
	}
}
Böyle olabilir . 

erg::MyNamespace::a::x = 200;


namespace nec {
	int x = 5;
	void foo();
}

namespace nec {
	int y = 10;
	void bar();
}


Namespace ler birleþtirilebilir. yani kümülatif toplanýr.

Farklý header dosyalarýnda ayný namespace i açýp
deðiþik þeyler tanýmlayabiliriz.
ve sonunda hepsi tek bir namespace altýnda toplanýr.
Kullanacaðýmýz modulü dahil ederek örneðin nec::adc()
nec::spi() gibi kullanabiliriz.

std::vector
std::string
std::list


nested namespace
using declaration  using std::cout gibi.
using directive		using namespace std; gibi.
ADL (Argument Dependent Lookup)
inline namespace
namespace alias
unnamed namespace
